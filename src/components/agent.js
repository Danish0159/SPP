// import axios from "axios";

// axios.defaults.baseURL = "http://df2b-103-125-176-197.ngrok.io/";

// const responseBody = (response) => response.data;

// const requests = {
//   post: (url, body) => axios.post(url, body).then(responseBody),
// };

// const carrier = {
//   addCarrier: (carrierInfo) => requests.post("/user/signup/", carrierInfo),
// };

// export default { carrier };
